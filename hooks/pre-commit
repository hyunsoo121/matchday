#!/bin/sh

echo "▶ Spotless 코드 포맷 검사 중..."

./gradlew spotlessCheck --daemon -q
STATUS=$?

if [ $STATUS -ne 0 ]; then
  echo ""
  echo "✗ 포맷 오류가 있습니다. 아래 명령어로 자동 수정 후 다시 커밋하세요:"
  echo "  ./gradlew spotlessApply"
  echo ""
  exit 1
fi

echo "✓ 포맷 검사 통과"
exit 0
